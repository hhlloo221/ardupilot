# 项目协作规则

## 沟通与文件操作

- 默认使用中文回复。
- 不随意删除文件；确需删除时，先说明原因并征得用户确认。

## 代码验证限制

- 修改本项目代码后，不得执行任何编译、构建、链接或固件生成命令。
- 不得以编译作为验证手段，包括但不限于 `python waf`、`./waf`、`make`、`ninja`、`cmake --build` 等命令。
- 仅允许采用不会触发编译的静态检查，例如审阅代码差异、执行 `git diff --check`、搜索符号引用和检查配置文本。
- 完成代码修改后，应明确告知用户“未进行编译验证”。
- 只有用户在当前任务中明确要求解除本限制时，才可以执行编译或构建命令。
